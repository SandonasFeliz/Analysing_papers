name: ta_env

channels:
  
  - defaults

dependencies:
  - python=3.11
  - pip
  - pip:
      - certifi
      - charset-normalizer
      - contourpy
      - cycler
      - fonttools
      - idna
      - kiwisolver
      - lxml
      - matplotlib
      - numpy
      - pillow
      - pyparsing
      - python-dateutil
      - requests
      - six
      - urllib3
      - wordcloud
